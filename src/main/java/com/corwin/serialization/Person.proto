syntax = "proto3";  // 版本
option java_outer_classname = "PersonProtobuf"; // 生成的外部类名，同时也是文件名
// protobuf 使用message 管理数据
message Person {  // 会在 Person 外部类生成一个内部类 Person，它是真正发送的POJO对象
  int32 age = 1;
  string name = 2;
}